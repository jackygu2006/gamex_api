export const apiBaseUrl = "/api/v1";
export const isHttps = true; // Whever support https, always support http rest api
export const httpsPort = 8443; // Port if support https
export const httpPort = 8088;
